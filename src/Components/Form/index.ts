export default interface Values {
    email: string;
    password: string;
    confPassword?: string;
    firstName?: string;
    lastName?: string;
    tel?: string;
    phoneNumber?: string;
    
  }